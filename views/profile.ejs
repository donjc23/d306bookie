<!-- views/profile.ejs -->
<!doctype html>
<html>
<head>
	<title>D306 bookie</title>
	<link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.0.2/css/bootstrap.min.css">
	<link rel="stylesheet" href="//netdna.bootstrapcdn.com/font-awesome/4.0.3/css/font-awesome.min.css">
	<style>
		body 		{ padding-top:80px; word-wrap:break-word; }
	</style>
</head>
<body>
<div class="container">

	<div class="page-header text-center">
		<h1><span class="fa fa-anchor"></span> Profile Page</h1>
		<p>You can only pick ONCE!! <br>
		   Must be picked by 7pm!!!
		</p>
		<a href="/logout" class="btn btn-default btn-sm">Logout</a>
                <% if (name == 'donky') { %>
                	<form action="/score" method="post">

						<hr>
					    <button type="submit" class="btn btn-warning btn-lg">cal score</button>
					    
				    </form>	
				 <% } %>
	</div>

	<div class="row">
		<div class="well">
        
			<% if (lastPicks.length>0)  { %>
				<h4> Your picks from last night : </h4>
                    <ul>
						<% for(var i=0; i<lastPicks.length; i++) {%>
   							<li><%= lastPicks[i] %></li>
						<% } %>
					</ul>
			<% } %>
         
    	</div>
    </div>

	<div class="row">

		<!-- LOCAL INFORMATION -->
        <div class="col-sm-6">
            <div class="well">
                <h3><span class="fa fa-user"></span> Date is <%= day %></h3>

                <% if (picks.length==0 && sevenOclock==false) { %>
                	<form action="/profile" method="post">

	    				<h1>todays matches</h1>

						<label class="radio-inline">
				      	<input type="radio" name="match1" value="Knicks+9.5" >Knicks Vs
				    	</label>
				    	<label class="radio-inline">
				      	<input type="radio" name="match1" value="Cavaliers-9.5" checked="checked" >Cavaliers-9.5
				    	</label>
						<hr>
						<label class="radio-inline">
				      	<input type="radio" name="match2" value="Jazz+5.5" >Jazz Vs
				    	</label>
				    	<label class="radio-inline">
				      	<input type="radio" name="match2" value="Trail Blazers-5.5" checked="checked" >Trail Blazers-5.5
				    	</label>
						<hr>
						<label class="radio-inline">
				      	<input type="radio" name="match3" value="Spurs+9" >Spurs Vs
				    	</label>
				    	<label class="radio-inline">
				      	<input type="radio" name="match3" value="Warriors-9" checked="checked" >Warriors-9
				    	</label>
				    	
						<hr>
					    <button type="submit" class="btn btn-warning btn-lg">Pick'em</button>
					    
				    </form>
				 <% } else if(picks.length>0){ %>
				 
                	<h4> Today you picked: </h4>
                    <ul>
						<% for(var i=0; i<picks.length; i++) {%>
   							<li><%= picks[i] %></li>
						<% } %>
					</ul>
				<% } else { %>
             		<h4> Sorry buddy, you missed boat today! </h4>
             <% } %>

            </div>
        </div>

	</div>

</div>
</body>
</html>
